import React from 'react';

const Header = () => {
    return(
        <h1 style={headerStyle}>Reframe Flow Checklist</h1>
    )
}

const headerStyle = {
    textAlign : "center",
    fontSize : "60px",
    marginTop : "-10px"
}

export default Header;